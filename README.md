# ZenGeekLabs.github.io

一个基于 `Hugo` 和 `PaperMod` 的个人博客。

## 本地开发

```bash
hugo server
```

默认访问地址：

```bash
http://127.0.0.1:1313
```

## 目录结构

- `content/` 博客文章与页面内容
- `themes/PaperMod/` 主题文件
- `static/` 站点静态资源
- `hugo.toml` 站点配置
